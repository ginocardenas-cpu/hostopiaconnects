# Asset files go here

Drop your **docs** (PDF, etc.) and **videos** (MP4, etc.) in this folder.

Then add matching entries to the catalog in **`lib/assets.ts`** (see project root **`ADDING-CONTENT.md`**).

Example: put `my-deck.pdf` here → in `lib/assets.ts` set `fileUrl: "/assets/my-deck.pdf"`.
