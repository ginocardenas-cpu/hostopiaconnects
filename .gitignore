# Dependencies
node_modules
.next
out

# Large media in the HTML catalog folder (GitHub 100MB limit)
public/assets/*.mp4
public/assets/*.mov
public/assets/*.webm

# Companion videos for Connects Video assets live under public/videos/ (tracked)

# Env
.env*.local
.env

# TypeScript incremental build cache (local only)
*.tsbuildinfo

# Local Claude Design bundle extract (not source of truth)
_design-extract/

# Local scratch from inventory / xlsx inspection (do not commit)
_inv.zip
_xlsx_unzip/

# Excel lock files (open workbook)
~$*.xlsx

# Misc
.DS_Store
*.pem
.vercel

.vercel
.env*
