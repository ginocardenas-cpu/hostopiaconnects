# Dependencies
node_modules
.next
out

# Large media (GitHub 100MB limit) – keep README and .gitkeep
public/assets/*.mp4
public/assets/*.mov
public/assets/*.webm

# Env
.env*.local
.env

# TypeScript incremental build cache (local only)
*.tsbuildinfo

# Local Claude Design bundle extract (not source of truth)
_design-extract/

# Local scratch from inventory / xlsx inspection (do not commit)
_inv.zip
_xlsx_unzip/

# Misc
.DS_Store
*.pem
.vercel
